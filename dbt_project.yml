name: 'dbt_semantic_poc'
version: '1.0.0'
config-version: 2

profile: 'dbt_semantic_poc'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_semantic_poc:
    staging:
      +materialized: view
      +schema: dev_cf_ebi_semantic_poc
    semantic:
      +materialized: view
      +schema: dev_cf_ebi_semantic_poc
